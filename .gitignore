/target
Cargo.lock
**/*.rs.bk
*.pdb
.idea/
.vscode/
*.swp
*.swo

.claude/skills/

# Project-meta audit artifacts live in the meta-repo (audit-archive/).
audit/
CORE-PARITY-AUDIT/

# runtime setban list (written on setban; must not dirty the tree / block the bot)
crates/rpc/banlist.json
